apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itom-service-discovery.fullname" . }}-test-connection"
  labels:
{{ include "itom-service-discovery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "itom-service-discovery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
