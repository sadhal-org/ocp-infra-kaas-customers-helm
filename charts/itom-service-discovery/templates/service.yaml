apiVersion: v1
kind: Service
metadata:
  name: {{ include "itom-service-discovery.fullname" . }}
  labels:
    {{- include "itom-service-discovery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "itom-service-discovery.selectorLabels" . | nindent 4 }}
