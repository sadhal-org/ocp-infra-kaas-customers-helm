apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aimlinfra-monitoring.fullname" . }}-test-connection"
  labels:
{{ include "aimlinfra-monitoring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "aimlinfra-monitoring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
