apiVersion: v1
kind: Service
metadata:
  name: {{ include "aimlinfra-monitoring.fullname" . }}
  labels:
    {{- include "aimlinfra-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aimlinfra-monitoring.selectorLabels" . | nindent 4 }}
