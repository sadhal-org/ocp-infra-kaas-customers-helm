apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ipapcip-amq-streams-operator.fullname" . }}-test-connection"
  labels:
{{ include "ipapcip-amq-streams-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ipapcip-amq-streams-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
