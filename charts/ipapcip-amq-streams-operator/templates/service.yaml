apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipapcip-amq-streams-operator.fullname" . }}
  labels:
    {{- include "ipapcip-amq-streams-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ipapcip-amq-streams-operator.selectorLabels" . | nindent 4 }}
