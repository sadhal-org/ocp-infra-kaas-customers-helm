apiVersion: v1
kind: Service
metadata:
  name: {{ include "aimlinfra-kuberay-operator.fullname" . }}
  labels:
    {{- include "aimlinfra-kuberay-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aimlinfra-kuberay-operator.selectorLabels" . | nindent 4 }}
