apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aimlinfra-kuberay-operator.fullname" . }}-test-connection"
  labels:
{{ include "aimlinfra-kuberay-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "aimlinfra-kuberay-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
